<div class='reviews'>
<%= form_for(@comentario) do |f| %>

    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :articulo_id, value: articulo%>
    <%= f.text_area :content, placeholder: "Máximo 140 caracteres...", :rows=> 10, :cols=>100 %>

  <div class="actions">
    <%= f.submit "Agregar comentario" %>
  </div>
<% end %>
</div>
